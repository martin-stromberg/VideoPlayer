<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:views="clr-namespace:VideoMeister.Views"
             x:Class="VideoMeister.Views.SourceNavigationPanel">

    <ScrollView>
        <FlexLayout
        AlignContent="Start"
        AlignItems="Start"
        AlignSelf="Start"
        BindableLayout.ItemsSource="{Binding Items}"
        Direction="Row"
        JustifyContent="Start"
        Wrap="Wrap">
            <BindableLayout.ItemTemplate>
                <DataTemplate>
                    <Grid
                    Margin="5"
                    Padding="10"
                    ColumnSpacing="10"
                    FlexLayout.Basis="240"
                    FlexLayout.Grow="1"
                    FlexLayout.Shrink="0"
                    HorizontalOptions="FillAndExpand">
                        <views:MediaElementBox/>
                    </Grid>
                </DataTemplate>
            </BindableLayout.ItemTemplate>
        </FlexLayout>
    </ScrollView>

</ContentView>
