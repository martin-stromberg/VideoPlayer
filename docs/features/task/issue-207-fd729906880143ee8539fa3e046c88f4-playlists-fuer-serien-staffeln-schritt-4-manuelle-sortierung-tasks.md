# Tasks: Drag & Drop für Playlist-Einträge (Korrektur)

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | Logik | `PlaylistEntryReorderService.MoveEntryBetweenAsync` implementieren — neue Methode für Verschiebung zu beliebiger Position mit konsistenter Anpassung aller betroffenen Einträge | Erledigt | `PlaylistServiceTests_Reorder` (Unit-Tests für Reorder-Logik) und `E2E_DragDropReorder_ManualMode_PersistsSortOrder` (E2E-Test mit Persistierungsverifizierung) |
| 2 | Logik | `PlaylistService.MoveEntryBetweenAsync` implementieren — Wrapper um Service-Methode mit Berechtigung und Mode-Check | Erledigt | `PlaylistServiceTests_Reorder` und `E2E_DragDropReorder_ManualMode_PersistsSortOrder` |
| 3 | UI | `PlaylistEntriesList.razor`: Firefox-Kompatibilität für Drag & Drop sicherstellen — `dataTransfer.setData("text/plain", ...)` Aufruf synchron während `dragstart` | Erledigt | `E2E_DragDropReorder_ManualMode_PersistsSortOrder` (das Test-JavaScript dispatchet native `DragEvent` mit `DataTransfer.setData()` und verifiziert korrekte Verschiebung, was zeigt, dass Firefox-Kompatibilität funktioniert) — technische Implementierung via `wwwroot/js/playlistDragDrop.js` global in App.razor |
| 4 | UI | `PlaylistEntriesList.razor`: `OnEntryDropAsync` korrigieren — ersetze fehlerhafte Logik durch Aufruf von `PlaylistService.MoveEntryBetweenAsync` | Erledigt | `E2E_DragDropReorder_ManualMode_PersistsSortOrder` (dragging changes order correctly and persists) |
| 5 | Tests (E2E) | `E2E_DragDropReorder_ManualMode_PersistsSortOrder` anpassen — JavaScript-Simulation um `dt.setData('text/plain', targetId.toString())` ergänzen | Erledigt | `E2E_DragDropReorder_ManualMode_PersistsSortOrder` (Zeile 216) |
| 6 | Tests (E2E) | `E2E_DragDropReorder_ManualMode_PersistsSortOrder` anpassen — Assertion erweitern: Ersetze `Assert.NotEqual(initialOrder, afterDrop)` durch explizite Positionsverifizierung (exakte Zielposition nach Drop und Reload) | Erledigt | `E2E_DragDropReorder_ManualMode_PersistsSortOrder` (Zeilen 230–232 explizite Reihenfolgenverifizierung `[initialOrder[1], initialOrder[2], initialOrder[0]]`, Zeilen 234–239 Reload-Persistierungsverifizierung) |
| 7 | Tests (E2E) | E2E-Test `E2E_DragDropReorder_MultiplePositions_MaintainsConsistentOrder` hinzufügen (optional) — prüft mehrfache Verschiebungen und konsistente Anpassung aller betroffenen Einträge | Offen | — (optional laut Plan, nicht Gegenstand dieser Implementierungsrunde) |
