# Tasks: Geräte-Pairing mit dynamischem Geräte-Token

| # | Bereich | Aufgabe | Status | Testnachweis |
|---|---------|---------|--------|--------------|
| 1 | Datenmodell | Entität `PairedDevice` anlegen (`VideoWebPlayer/Data/PairedDevice.cs`: `Id`, `Name`, `TokenHash`, `IssuedAtUtc`, `LastUsedAtUtc`, `RevokedAtUtc`, `CreatedByUserId`) | Erledigt | `dotnet build VideoPlayer.sln` fehlerfrei; genutzt in `DeviceTokenServiceTests`/`PairingServiceTests_*` |
| 2 | Datenmodell | Entität `PairingCode` anlegen (`VideoWebPlayer/Data/PairingCode.cs`: `Id`, `CodeHash`, `CreatedAtUtc`, `ExpiresAtUtc`, `ConsumedAtUtc`, `CreatedByUserId`) | Erledigt | `dotnet build VideoPlayer.sln` fehlerfrei; genutzt in `PairingServiceTests_*` |
| 3 | Datenmodell | `PairedDeviceConfiguration` anlegen (`IEntityTypeConfiguration<PairedDevice>`: Unique-Index `TokenHash`, Index `RevokedAtUtc`, `HasMaxLength`) | Erledigt | Migration `AddDevicePairing` enthält `IX_PairedDevices_TokenHash` (unique) und `IX_PairedDevices_RevokedAtUtc` |
| 4 | Datenmodell | `PairingCodeConfiguration` anlegen (`IEntityTypeConfiguration<PairingCode>`: Index `CodeHash`, Index `ExpiresAtUtc`, `HasMaxLength`) | Erledigt | Migration `AddDevicePairing` enthält `IX_PairingCodes_CodeHash` und `IX_PairingCodes_ExpiresAtUtc` |
| 5 | Datenmodell | `DbSet<PairedDevice>` und `DbSet<PairingCode>` in `ApplicationDbContext` ergänzen | Erledigt | `dotnet build` fehlerfrei; Service-Tests nutzen die DbSets auf In-Memory-SQLite |
| 6 | Datenmodell | EF-Migration `AddDevicePairing` erzeugen (`dotnet ef migrations add`, `VideoWebPlayer/Migrations/`) | Erledigt | `VideoWebPlayer/Migrations/20260923074815_AddDevicePairing.cs` (+ Designer/Snapshot); E2E-Tests laufen gegen `MigrateDatabase()` |
| 7 | Vertrag / DTOs | `PairingExchangeRequest` in `VideoWebPlayer.Client/Models/` anlegen (`Code`, `ClientPublicKey`, `DeviceName?`; verbindliche JSON-Feldnamen camelCase: `code`, `clientPublicKey`, `deviceName`) | Erledigt | `PairingExchangeContractTests_JsonContract.Exchange_UsesBindingJsonFieldNames` (Roh-JSON) bestanden |
| 8 | Vertrag / DTOs | `PairingExchangeResponse` in `VideoWebPlayer.Client/Models/` anlegen (`ServerPublicKey`, `EncryptedToken`; verbindliche JSON-Feldnamen: `serverPublicKey`, `encryptedToken`) | Erledigt | `PairingExchangeContractTests_JsonContract.Exchange_UsesBindingJsonFieldNames` bestanden |
| 9 | Logik | `IDeviceTokenService`/`DeviceTokenService` implementieren (`IssueAsync`, `IsValidDeviceTokenAsync` mit `LastUsedAtUtc`-Update, `RevokeAsync`, `GetDevicesAsync`; SHA-256-`TokenHash`, Base64Url-Zufallstoken) | Erledigt | `DeviceTokenServiceTests` (6 Tests) bestanden |
| 10 | Logik | `IPairingService`/`PairingService` implementieren (`CreatePairingCodeAsync`, `ExchangeAsync` mit atomarem Code-Verbrauch, ECDH P-256/SPKI, AES-256-GCM, `GetActiveCodesAsync`; `PairingExchangeResult`) | Erledigt | `PairingServiceTests_*` (11 Tests) bestanden |
| 11 | Konfiguration | `Pairing:CodeLength` (Default 8) und `Pairing:CodeTtlMinutes` (Default 5) via `IConfiguration` im `PairingService` lesen; Code-Alphabet als Konstante | Erledigt | `PairingServiceTests_CodeCreation` prüft Länge/Alphabet/TTL |
| 12 | Logik | Services in `ServiceCollectionExtensions.AddVideoWebPlayerServices` registrieren (`AddScoped<IDeviceTokenService, DeviceTokenService>()`, `AddScoped<IPairingService, PairingService>()`) | Erledigt | Vertragstests via `WebApplicationFactory<Program>` lösen beide Services auf (kein DI-Fehler) |
| 13 | Logik | `PairingController` anlegen (`POST api/pairing/exchange`, keine Gate-Attribute, `ILoginIpBlockService`-Prüfung → 429, Validierung → 400/401, Response-Mapping) | Erledigt | `PairingExchangeContractTests_*` (8 Tests) bestanden |
| 14 | Logik | `ApiTokenCheckAttribute` auf `OnActionExecutionAsync` umstellen und bei `MauiOnly` Geräte-Token-Prüfung über `IDeviceTokenService` aus `RequestServices` ergänzen (`AnyClient` unverändert) | Erledigt | `ApiTokenConfigurationTests` bestanden (Geräte-Token akzeptiert, widerrufen → 401, Config-Fallback, `AnyClient` ohne DB) |
| 15 | UI | Admin-Seite `Devices.razor` erstellen (`/admin/devices`, `InteractiveServer`, `IsAdmin`-Check nach `Security.razor`-Muster, `admin-*`-CSS) | Erledigt | `DevicePairingE2ETests.Admin_Creates_Pairing_Code_And_Sees_Code_With_Ttl` + `NonAdmin_Sees_NotAuthorized_On_Devices_Page` bestanden |
| 16 | UI | `Devices.razor`: Pairing-Code-Erzeugung mit TTL-Anzeige und Liste aktiver Codes | Erledigt | `DevicePairingE2ETests.Admin_Creates_Pairing_Code_And_Sees_Code_With_Ttl` bestanden |
| 17 | UI | `Devices.razor`: Geräteliste (`Name`, `IssuedAtUtc`, `LastUsedAtUtc`, Status) mit Widerruf-Aktion | Erledigt | `DevicePairingE2ETests.Revoked_Device_Token_Login_Fails` bestanden |
| 18 | UI | Kachel „Geräte" (`/admin/devices`) in `AdminIndex.razor` ergänzen | Erledigt | `dotnet build` fehlerfrei; Kachel in `AdminIndex.razor` vorhanden |
| 19 | Tests | `DeviceTokenServiceTests` anlegen (Issue/Hash, Validierung inkl. `LastUsedAtUtc`, Widerruf, Auflistung; In-Memory-SQLite nach `ApplicationDbContextTests`-Muster) | Erledigt | 6 Tests bestanden (`dotnet test --filter "Category!=E2E"`) |
| 20 | Tests | `PairingServiceTests` anlegen (Code-Erzeugung, Exchange-Erfolg mit ECDH-Roundtrip, unbekannter/abgelaufener/verbrauchter Code, Race beim Verbrauch, ungültiger PublicKey, PublicKey falscher Kurve ≠ `nistP256`, leerer/fehlender `Code` → Formatfehler ohne Fehlversuchszählung, `DeviceName` > 200 Zeichen abgelehnt, `GetActiveCodesAsync` nur aktive Metadaten ohne Hash/Klartext) | Erledigt | `PairingServiceTests_CodeCreation`/`_Exchange`/`_ActiveCodes` — 11 Tests bestanden |
| 21 | Tests | `ApiTokenConfigurationTests` erweitern (`MauiOnly` akzeptiert Geräte-Token, verworfenes Token → 401, Config-Fallback weiterhin gültig, `AnyClient` ohne DB-Zugriff) | Erledigt | Alle `ApiTokenConfigurationTests` bestanden |
| 22 | Tests | `ApiTokenCheckAttribute_InvalidTokenLogDoesNotIncludeHeaderValue` auf `OnActionExecutionAsync`-Aufruf umstellen | Erledigt | Test bestanden (Unauthorized + kein Token im Log) |
| 23 | Tests | `PairingExchangeContractTests` anlegen (öffentlicher Endpunkt ohne Auth, 401 bei ungültigem Code, 429 nach wiederholten Fehlversuchen, 400 bei `deviceName` > 200 Zeichen, 400 bei fehlendem/leerem `code` ohne Fehlversuchszählung, 400 bei falscher Kurve, verbindliche camelCase-JSON-Feldnamen via Roh-JSON, entschlüsseltes Token → `POST /api/auth/login` → 200) | Erledigt | `PairingExchangeContractTests_Flow`/`_Validation`/`_JsonContract` — 8 Tests bestanden |
| 24 | Tests | `ApiDocumentationContractTests.ApiDocumentationContainsMauiRelevantRoutes` um `POST /api/pairing/exchange` erweitern | Erledigt | `ApiDocumentationContainsMauiRelevantRoutes` bestanden |
| 25 | E2E-Tests | `DevicePairingE2ETests`: Admin-Login → `/admin/devices` → Pairing-Code erzeugen, Code + TTL sichtbar | Erledigt | `Admin_Creates_Pairing_Code_And_Sees_Code_With_Ttl` bestanden |
| 26 | E2E-Tests | `DevicePairingE2ETests`: Gesamtfluss Code aus UI → ECDH-Exchange per HttpClient → Login mit Geräte-Token erfolgreich | Erledigt | `Pairing_Flow_UiCode_Exchange_Login_Succeeds` bestanden |
| 27 | E2E-Tests | `DevicePairingE2ETests`: Widerruf in UI → Login mit Geräte-Token → 401 | Erledigt | `Revoked_Device_Token_Login_Fails` bestanden |
| 28 | E2E-Tests | `DevicePairingE2ETests`: ungültiger Code → Fehler; wiederholte Fehlversuche → IP-Sperre sichtbar in `/admin/security` (beide Teilnachweise im selben Test) | Erledigt | `Invalid_Codes_Block_Ip_And_Show_In_Security` bestanden |
| 29 | E2E-Tests | `DevicePairingE2ETests`: Benutzer ohne `IsAdmin`-Claim öffnet `/admin/devices` → „Nicht autorisiert.", keine Code-Erzeugung/Geräteliste/Widerruf-Aktionen gerendert | Erledigt | `NonAdmin_Sees_NotAuthorized_On_Devices_Page` bestanden |
| 30 | Dokumentation | `docs/API.md`: `POST /api/pairing/exchange` (inkl. verbindlicher JSON-Feldnamen und Krypto-Parameter), geänderte `X-API-Key`-Semantik (Geräte-Tokens + `Jwt:ApiToken:Maui`-Fallback) und Widerruf-Verhalten (ausgestellte Benutzer-JWTs bleiben bis zum Ablauf von 12 h gültig) dokumentieren | Erledigt | `ApiDocumentationContainsMauiRelevantRoutes` bestanden; Abschnitte in `docs/API.md` vorhanden |
| 31 | Dokumentation | `docs/GUIDE_Installation.md`: Abschnitt Geräte-Pairing inkl. `Pairing:`-Konfiguration ergänzen | Erledigt | Abschnitt „Geräte-Pairing" in `docs/GUIDE_Installation.md` vorhanden |
| 32 | Dokumentation | `docs/help/einrichtung.md` aktualisieren (Kachel „Geräte"); ggf. neue Hilfeseite zum Pairing-Ablauf inkl. Hinweis auf das Widerruf-Verhalten (JWT-Restlaufzeit 12 h) | Erledigt | `docs/help/einrichtung.md` enthält Kachel + Pairing-Ablauf inkl. Widerruf-/JWT-Hinweis |
| 33 | Dokumentation | `docs/SECRETS_MANAGEMENT.md`: `Jwt:ApiToken:Maui` als Fallback zum Geräte-Token-Verfahren ergänzen | Erledigt | `Jwt:ApiToken:Maui` als Fallback in `docs/SECRETS_MANAGEMENT.md` dokumentiert |
| 34 | Review (Code) | `HashHelper.Sha256Hex` in `Services/Security/` anlegen; `DeviceTokenService.ComputeHash` entfernen und alle Referenzen (Services + Tests) auf `HashHelper` umstellen | Erledigt | `dotnet build` fehlerfrei; Hash-Assertions in `DeviceTokenServiceTests`/`PairingServiceTests_*` nutzen `HashHelper.Sha256Hex` |
| 35 | Review (Code) | `PairingService.ExchangeAsync` aufteilen (`TryImportClientPublicKey`, `EncryptToken`); Ablaufbedingung `ExpiresAtUtc > now` in die atomare `ExecuteUpdateAsync`-Where-Klausel aufnehmen | Erledigt | `PairingServiceTests_Exchange` (inkl. `ExchangeAsync_ExpiredCode_Fails` mit `ConsumedAtUtc`-Invariante und `ExchangeAsync_ConcurrentConsume_OnlyOneSucceeds`) bestanden |
| 36 | Review (Code) | `CreatePairingCodeAsync` gibt `CreatedPairingCode` (Klartext-Code + persistiertes `ExpiresAtUtc`/`CreatedAtUtc`) zurück; `Devices.razor` zeigt Server-Wert statt Config-Lesen/TTL-Schätzung | Erledigt | `PairingServiceTests_CodeCreation.CreatePairingCodeAsync_ReturnsCodeAndPersistsHash` prüft `created.ExpiresAtUtc` == persistierter Wert; E2E TTL-Anzeige bestanden |
| 37 | Review (Code) | Test-Hygiene: `PairingTestDb.Dispose` gibt `_provider` frei (ohne `?.`); leere Catch-Blöcke in `PairingExchangeContractTestBase`/`DevicePairingE2ETests` auf `IOException`/`UnauthorizedAccessException` eingeschränkt; `PairingServiceTests_CodeCreation.CreateService` auf eine `ApplicationDbContext`-Überladung reduziert; `ApiTokenConfigurationTests` löst Scoped-Services via `IServiceScope` auf | Erledigt | `dotnet build` fehlerfrei; alle betroffenen Tests bestanden |
| 38 | Review (Usability) | `Devices.razor`: Spalte „Ersteller" löst `CreatedByUserId` via `UserManager<ApplicationUser>` zur E-Mail-Adresse auf | Erledigt | E2E `Admin_Creates_Pairing_Code_And_Sees_Code_With_Ttl` prüft Sichtbarkeit der Admin-E-Mail in der Codes-Tabelle |
| 39 | Review (Usability) | `IDeviceTokenService.RenameAsync(deviceId, newName)` implementieren (Trim, 1–200 Zeichen) + Umbenennen-Aktion (Inline-Edit) in `Devices.razor` | Erledigt | `DeviceTokenServiceTests.RenameAsync_*` (4 Tests) + E2E `Admin_Renames_Paired_Device` bestanden |
| 40 | Review (Code) | Test-Helfer auslagern: `PairingCryptoHelper.DecryptToken` neu (`Helpers/`), `PairingServiceTests_CodeCreation.CreateService` → `PairingTestDb.CreatePairingService()`, `GetService`+Cast → `GetRequiredService<ApplicationDbContext>()` | Erledigt | `dotnet build` fehlerfrei; alle `PairingServiceTests_*`/`PairingExchangeContractTests_*`/`DevicePairingE2ETests` bestanden |
| 41 | Review (Code) | `PairingWebApplicationFactory` in `Helpers/` anlegen (Temp-DB-Pfad + gemeinsame Settings + `HttpsRedirectionOptions`); Duplikat in `PairingExchangeContractTestBase`/`DevicePairingE2ETests` entfernt | Erledigt | Vertrags- und E2E-Tests bestanden gegen die gemeinsame Factory |
| 42 | Review (Code) | `TryImportClientPublicKey`: `ExportParameters`/Oid-Prüfung in den `try`-Block aufgenommen, `PlatformNotSupportedException` zum Catch-Filter ergänzt → kontrolliertes 400 statt 500 | Erledigt | `PairingServiceTests_Exchange.ExchangeAsync_ExplicitParametersPublicKey_Fails` (SPKI mit SpecifiedECDomain) bestanden |
| 43 | Review (Code) | `ExchangeAsync` trimmt `DeviceName` vor der Längenprüfung (konsistent zu `RenameAsync`) und übergibt den getrimmten Wert an `IssueAsync` | Erledigt | `PairingServiceTests_Exchange.ExchangeAsync_DeviceNameWithPaddingWithinTrimmedLimit_Succeeds` bestanden |
| 44 | Review (Code) | `LoginIpBlockService.Unblock` entfernt den Cache-Eintrag der IP immer (setzt auch Sub-Threshold-Fehlerzähler zurück); `Exchange_Returns400OnMissingOrEmptyCode` räumt per `UnblockIp` in `finally` auf | Erledigt | `LoginIpBlockServiceTests.Unblock_SubThresholdFailures_ResetsFailureCount` bestanden |
| 45 | Review (Usability) | `Devices.razor`: alle Zeitangaben via `ToLocalTime()` (Muster `Backups.razor`/`Updates.razor`), UTC-Labels aus den Spaltenköpfen entfernt | Erledigt | E2E `Admin_Creates_Pairing_Code_And_Sees_Code_With_Ttl` bestanden |
| 46 | Review (Usability) | `DeviceTokenService.IssueAsync`: unterscheidbarer Fallback-Name „Geraet vom dd.MM.yyyy, HH:mm:ss" statt pauschal „Geraet" | Erledigt | `DeviceTokenServiceTests.IssueAsync_*_UsesDefaultName` (2 Tests) bestanden |
| 47 | Review (Usability) | `Devices.razor`: Widerruf-Bestätigung nach `Backups.razor`-Muster (Checkbox + „Widerruf bestaetigen"/„Abbrechen", `data-testid='revoke-confirmation'`) | Erledigt | E2E `Revoked_Device_Token_Login_Fails` (angepasst) + neu `Revoke_Requires_Confirmation_And_Can_Be_Cancelled` bestanden |

## Testlauf-Zusammenfassung

- `dotnet build VideoPlayer.sln` — 0 Fehler.
- `dotnet test VideoWebPlayer.Tests/VideoWebPlayer.Tests.csproj -c Release --filter "Category!=E2E"` — **341/341 bestanden** (nach Review-Iteration 3: +4 Tests: `Unblock_SubThresholdFailures_ResetsFailureCount`, `ExchangeAsync_ExplicitParametersPublicKey_Fails`, `ExchangeAsync_DeviceNameWithPaddingWithinTrimmedLimit_Succeeds`, `IssueAsync_WhitespaceDeviceName_UsesDefaultName`).
- `dotnet test VideoWebPlayer.Tests/VideoWebPlayer.Tests.csproj -c Release --filter "Category=E2E"` — **60/60 bestanden** (davon 7 `DevicePairingE2ETests` inkl. neuem `Revoke_Requires_Confirmation_And_Can_Be_Cancelled`; `Revoked_Device_Token_Login_Fails` an den Bestaetigungsdialog angepasst).
- Gesamt: 401 Tests, 0 Fehler, 0 übersprungen.
