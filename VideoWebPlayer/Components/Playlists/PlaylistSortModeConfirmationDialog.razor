<ConfirmationDialog Title="Sortiermodus aendern" OnConfirm="OnConfirm" OnCancel="OnCancel"
                    ConfirmButtonText="Ja, aendern" ConfirmButtonId="confirm-sortmode-change-button" CancelButtonId="cancel-sortmode-change-button">
    <p>Die manuelle Reihenfolge wird geloescht und kann nicht wiederhergestellt werden. Moechten Sie fortfahren?</p>
</ConfirmationDialog>

@code {
    [Parameter]
    public EventCallback OnConfirm { get; set; }

    [Parameter]
    public EventCallback OnCancel { get; set; }
}
